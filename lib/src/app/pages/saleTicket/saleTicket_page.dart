import 'package:flutter/material.dart';

class SaleTicketPage extends StatelessWidget {
  static const String routeName = "/saleTicket";

  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: new AppBar(
        title: new Text("SaleTicket"),
      ),
      body: Container(),
    );
  }
}

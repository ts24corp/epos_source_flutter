import 'package:flutter/material.dart';

class SaleTicketHistoryPage extends StatelessWidget {
  static const String routeName = "/saleTicketHistory";

  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: new AppBar(
        title: new Text("SaleTicketHistory"),
      ),
      body: Container(),
    );
  }
}
